{
  "name": "member_id",
  "version": "1.0.0",
  "description": "Generating and validating member ids",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node ./dist/index.js",
    "test": "tsc && mocha --require babel-core/register --timeout 5000 --recursive"
  },
  "author": "Daniel Singer",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.17",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "typescript": "^5.0.4"
  }
}
